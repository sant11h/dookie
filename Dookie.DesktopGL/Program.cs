using Dookie.DesktopGL;

using var game = new GameLoop();
game.Run();

/*
 * V1
 * Components based architecture - In the works
 * Run server and client in the same executable
 * Collisions with the mouse click
 * Scene system (main menu, playing, high score)
 * Rules system (victory/lose condition)
 * Timer system
 * Scoring system
 *
 * V2
 * Lobby multiplayer (lobby scene)
 * 
 */
